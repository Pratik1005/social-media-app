import { Input } from '@chakra-ui/react';

const SearchBar = () => {
  return (
    <Input
      placeholder="Search insight"
      borderRadius="full"
      marginBottom={2}
      border="2px"
    />
  );
};

export { SearchBar };
